<%= form_for(@intencion) do |f| %>
  <% if @intencion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intencion.errors.count, "error") %> prohibited this intencion from being saved:</h2>

      <ul>
      <% @intencion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
