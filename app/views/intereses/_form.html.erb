<%= form_for(@interes) do |f| %>
  <% if @interes.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interes.errors.count, "error") %> prohibited this interes from being saved:</h2>

      <ul>
      <% @interes.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
